let 
    fun malarkey () =
        let val trustLevel = `{malarkey}`
        let 
            
            


    fun main () =
        let val thisNode = node (self ())
            val _ = printString ("Running client node with indentifier:" ^ thisNode)
            val _ = register ("malarkey", spawn malarkey, authority)
        in ()
        end

in main ()
end